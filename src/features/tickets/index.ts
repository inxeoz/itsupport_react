// Main ticket dashboard
export { default as TicketsDashboard } from './components/TicketDashboard';

// Add ticket component  
export { AddTicket } from './components/AddTicket';

// Types will be added as we create them
// export type { TicketData, TicketFilter } from './types';

// Hooks will be added as we create them
// export { useTickets, useTicketFilters } from './hooks';